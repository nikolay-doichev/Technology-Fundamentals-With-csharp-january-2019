using System;
using System.Linq;

namespace _07.Max_v2
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] numbers = Console.ReadLine()
                                    .Split(' ')
                                    .Select(int.Parse)
                                    .ToArray();

            int currentIndex = 0;
            int lenght = 0;

            for (int index = 0; index < numbers.Length; index++)
            {

            }
        }
    }
}
